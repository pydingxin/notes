# 对象转换
通过各种Get*得到的输入，如form/json
可方便地用Parse方法(或Get*Struct)转为结构体，并自定义转换中的映射关系。

# 映射关系
底层转换用gconv模块。
对用户可能输入零值的字段，可以使用指针区分。
## 默认规则
这里以json→struct为例。struct中首字母大写的字段参与匹配，先规范化字段名:
把struct/json字段名都转为小写，删除各自字段名中的中划线/下划线/空格。
然后，有匹配的struct字段被赋值，否则置为零值。