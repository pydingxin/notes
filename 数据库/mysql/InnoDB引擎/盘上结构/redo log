redo log保证事务持久性
数据从磁盘读到内存，在内存中修改，还没写回到磁盘，此时服务器崩溃，数据就会丢失。redo log就是为了解决这个问题。

redo log运行时，数据也要从内存写到磁盘。关键就是在事务提交前，把redo log持久化，用一个持久化保证另一个持久化。

redo log由内存写到磁盘，由参数innodb_flush_log_at_trx_commit控制频率，0或2每秒写一次盘，最多丢失一秒数据；1每个事务都会写盘，不会丢失数据。
