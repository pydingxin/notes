一个ip:port发来一个连接请求，mysql会为其创建一个线程，给它初始化id和上下文变量，为它分配缓存并开始工作，到它断开连接，这个从开始到结束的过程叫一个session/会话。

一个host就是ip确定的一台机子，比如localhost。

mysql最大连接数max_connections，大概不到两百个。一般web程序有并发的需求，会申请很多连接，不同请求共享复用这些连接。
如果开启的web程序过多，mysql可能拒绝连接，程序就会出问题。

