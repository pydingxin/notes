从节点宕机重启，applier读取mysql.slave_relay_log_info找到之前执行到的位置，重新开始本机工作。
但考虑日志丢失问题，这个表若未落盘，就不知道到哪里了。

直接设置底层 innodb_flush_log_at_trx_commit=1 让每次写入都落盘，是最安全的。

可以设置UPS脚本断电自动执行。但宕机也不一定是因为电源。
也可以作高层作逻辑检查，锁住主从库，看是否一致。