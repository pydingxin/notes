if boolean
	statement1
else
	statement2
--------------------------------------------------
begin
	statement
end

块，用于if-else、while等控制流
--------------------------------------------------
choose(@idx,'a','b','c') 
相当于数组下标，sql中没有数组，只有"逗号分隔的值列表"